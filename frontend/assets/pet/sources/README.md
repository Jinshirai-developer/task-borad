# 制作用の元画像

- `portraits/`：11枚。6種類の元サイズ画像（犬は6表情）。アプリには直下の `*-web.png` を使用します。
- `atlases/`：6枚。各24差分を含む透過前のv1シート。アプリは直下の `*-atlas-v2-alpha.png` を使用します。

再生成の参照や透過処理の入力なので、未使用画像として削除しないでください。`scripts/prepare-pet-atlases.py` の参照先もこの場所へ更新しています。公開・配信はしません。
